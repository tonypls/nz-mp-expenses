import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "NZ Parliamentary Expenses | Transparency Dashboard",
  description:
    "Interactive visualization of New Zealand MP and Minister transport and accommodation spending from 2008 to 2025. Explore quarterly expense data by party, member, and category.",
  keywords: [
    "New Zealand",
    "Parliament",
    "MP expenses",
    "transparency",
    "government spending",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
